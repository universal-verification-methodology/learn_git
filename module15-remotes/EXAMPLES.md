# Module 15 examples — Remotes, PRs & submodules (concepts)

Track A (real Git). Each folder was adapted from `learn_unix_git`.

## `team_workflow/`

See [`examples/team_workflow/README.md`](examples/team_workflow/README.md) if present.

**Try:**

```bash
cd module15-remotes/examples/team_workflow
ls -la
```

## `submodules/`

See [`examples/submodules/README.md`](examples/submodules/README.md) if present.

**Try:**

```bash
cd module15-remotes/examples/submodules
ls -la
```
