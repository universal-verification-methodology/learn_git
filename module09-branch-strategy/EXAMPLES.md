# Module 09 examples — Branch naming

Track A (real Git). Each folder was adapted from `learn_unix_git`.

## `branch_rename/`

See [`examples/branch_rename/README.md`](examples/branch_rename/README.md) if present.

**Try:**

```bash
cd module09-branch-strategy/examples/branch_rename
ls -la
```

## `branching/`

See [`examples/branching/README.md`](examples/branching/README.md) if present.

**Try:**

```bash
cd module09-branch-strategy/examples/branching
ls -la
```

## `team_workflow/`

See [`examples/team_workflow/README.md`](examples/team_workflow/README.md) if present.

**Try:**

```bash
cd module09-branch-strategy/examples/team_workflow
ls -la
```
