# Module 10 examples — Merge conflicts

Track A (real Git). Each folder was adapted from `learn_unix_git`.

## `conflicts/`

See [`examples/conflicts/README.md`](examples/conflicts/README.md) if present.

**Try:**

```bash
cd module10-git-conflicts/examples/conflicts
ls -la
```

## `merging/`

See [`examples/merging/README.md`](examples/merging/README.md) if present.

**Try:**

```bash
cd module10-git-conflicts/examples/merging
ls -la
```
