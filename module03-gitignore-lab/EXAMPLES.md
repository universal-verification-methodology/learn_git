# Module 03 examples — .gitignore patterns

Track A (real Git). Each folder was adapted from `learn_unix_git`.

## `gitignore/`

See [`examples/gitignore/README.md`](examples/gitignore/README.md) if present.

**Try:**

```bash
cd module03-gitignore-lab/examples/gitignore
ls -la
```
