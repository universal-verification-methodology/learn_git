# Module 11 examples — Rebase vs merge

Track A (real Git). Each folder was adapted from Track A examples.

## `rebase/`

See [`examples/rebase/README.md`](examples/rebase/README.md) if present.

**Try:**

```bash
cd module11-git-rebase-merge/examples/rebase
ls -la
```

## `merging/`

See [`examples/merging/README.md`](examples/merging/README.md) if present.

**Try:**

```bash
cd module11-git-rebase-merge/examples/merging
ls -la
```
