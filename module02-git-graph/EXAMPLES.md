# Module 02 examples — Graph, stage, commit (playground)

Track A (real Git). Each folder was adapted from Track A examples.

## `init_clone/`

See [`examples/init_clone/README.md`](examples/init_clone/README.md) if present.

**Try:**

```bash
cd module02-git-graph/examples/init_clone
ls -la
```

## `status_diff/`

See [`examples/status_diff/README.md`](examples/status_diff/README.md) if present.

**Try:**

```bash
cd module02-git-graph/examples/status_diff
ls -la
```

## `add_commit_log/`

See [`examples/add_commit_log/README.md`](examples/add_commit_log/README.md) if present.

**Try:**

```bash
cd module02-git-graph/examples/add_commit_log
ls -la
```
